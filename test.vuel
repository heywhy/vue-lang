class App {
  //names;
  names: string = "AYomide";
  App(name) {
    //this.name = name;
  }

  call(name) {
    //print this.names;
    print "app calls " + this.names;
  }
}

class Circle < App {
  area = 8901;
  Circle(radius) {
    super(90);
    this.radius = radius;
  }

  call() {
    //super.call("me");
    return "lover";
  }

  increment() {
    return this.area += 1;
  }

  static revolve() {
    print "I'm revolving";
  }
}


var app: App = App("app");
var circle: Circle = Circle("app");
print circle.call();
var name = circle.call() ? 19902 : circle.call;
var b = 20, c = 120, d;

print name, d != nil ? 100 : 30;

print name = "loveryaskk", b;
print c;

print circle.area;

for (var i = 0, j = 20; i < 5; i = i+1, j = circle.increment()) {}
print "i => " + i;
print "j => " + j;

var e = 20;
print e %= 3;

print "E => " + e;
Circle.revolve();
