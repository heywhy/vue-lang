class App {
  //names;
  names: string = "AYomide";
  App(name) {
    //this.name = name;
  }

  call(name) {
    //print this.names;
    print "app calls " + this.names;
  }
}

class Circle < App {
  area = 8901;
  Circle(radius) {
    super(90);
    this.radius = radius;
  }

  call() {
    //super.call("me");
    return "lover";
  }
}


var app: App = App("app");
var circle: Circle = Circle("app");
print circle.call();
var name = circle.call() ? 19902 : circle.call;
var b = 20, c = 120, d;

print name, d;

print name = "loveryaskk", b;
print c;
